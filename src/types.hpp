/* This file is a shared file between all the other files in the system's just to make thing's clean */
#ifndef TYPE_H_
#define TYPE_H_
#include <iostream>

typedef enum {
    DOC,
    STD,
    TA,
} ATTRIBUTE;




#endif // TYPE_H_

